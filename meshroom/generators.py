from meshroom.decorators import SetupFunction
from meshroom.model import Integration


def generate_setup_function(integration: Integration) -> SetupFunction | None:
    # TODO Generate setup function on the fly based on the integration and its product's YAML specs
    return None
